{
  "slug": "mica",
  "note": "mica (Mica — the BASE unit; RL / Supporter / Wind / Burst I, cd 20s, ammo 6, reloadFrames 141, chargeFrames 60, chargeMult 250, normalMult 61.3, burstGaugePerShot 1.4, SR rarity). ⚠ EXACT SLUG: `mica` is the RL/Wind Supporter original — `mica-snow-buddy` (SMG/Iron, aka \"msb\") is an ENTIRELY DIFFERENT unit; the slug-disambiguation lint flags every bare \"Mica\"/\"mica\" token for this pair (including the slug itself — the base unit has no approved nickname, so NO text form passes clean), and the confirmation is recorded here and in the spec/manual-review headers per the mary precedent. Kit-autonomy gauntlet 2026-08-05 — test-first re-derivation pinned by scripts/tests/units/mica.test.ts (groups M1–M7 + encoding pins). Key parse facts: the maxAmmoFlat flat-round parse ('2 round(s)' is the AMOUNT, 'for 10 sec' the duration — NOT durationShots); alliesTopAtk{count:2, byFinalAtk:true} with no excludeSelf; the burstCast FB-exempt nuke. SKILL1 'Wonderous Star' ('■ Activates when attacked 20 time(s). Affects self. DEF ▲ 39.18% for 10S.'): ENCODED kit-verbatim as an `attacked` trigger block (count 20, target self, defPct 39.18 durationSec 10), matching the makima/yulha encoding of the same primitive — owner ruling 2026-08-13, encode-consistency: the line ships even though it is inert today. The trigger is a counter of hits RECEIVED, and it is DORMANT BY MECHANISM rather than merely unmeasured: `recordAttack` has exactly ONE caller in the engine — the `manualAttacksByFrame` test hook — so the v1 sim (no incoming ally damage, boss never acts) cannot advance the counter in ANY production comp, roster-wide. The effect side (self defPct 39.18/10s) would be damage-INERT even if it fired (self DEF never feeds damage dealt). admi carries the IDENTICAL 'attacked 20 time(s)' S1 archetype and jackal/maiden/noise the family — those remain UNMODELED + ⚑, so the family is NOT yet uniform; bringing them across is a separate batched call, not something this unit's file decides. The nearest-wrong encoding — hitCount:20 on her OWN shots ('attacks' misread) — is pinned RED in the unit test (it applies the buff; the shipped model applies nothing). SKILL2 'Brave Star' (datamined skillCooldownsSec.skill2 = 20s; NO activation clause in the prose): ONE block, interval:20 trigger (the CD-driven skill2 convention — poli/himeno precedent: an internal-cooldown skill with no visible activation clause fires every CD seconds of battle, first at t=CD), target alliesTopAtk{count:2, byFinalAtk:true, NO excludeSelf} — the kit literally says 'Affects 2 allies with the highest final ATK' (byFinalAtk per the A3 literal-word rule; quency carries the IDENTICAL wording with the identical encoding; mast-style '(except the skill user)' exclusion would say so and does not) — two effects in one block: (1) 'Max Ammunition Capacity ▲ 2 round(s) for 10 sec' = maxAmmoFlat 2, durationSec 10 — the theme-14 FLAT-round primitive: '▲ 2 round(s)' is a MAGNITUDE in flat rounds, NOT a durationShots round-count duration and NOT a percent; maxAmmo() = round(base×(1+pct/100)) + flat, so the nearest-wrong maxAmmoPct 2 computes round(6×1.02) = 6 and silently never extends a magazine (pinned RED). LOAD-BEARING weapon-state modifier (hard rule 1 / prior 9): extended magazines → fewer reloads → more firing uptime + gauge for the holders (ada+helm on the fixture basis). (2) 'DEF ▲ 19.89% for 10 sec' = defPct 19.89, durationSec 10 — FAITHFUL but damage-INERT in v1 (novel/poli/crust/sakura/diesel/jackal precedent; the unit test proves byte-identical totals with ONLY this effect removed, and the atkPct misread moving totals). BURST 'Sparkling Star' (burstCast — HER OWN cast, never fullBurstEnter: as the sole B1 she casts every Full Burst so both keyings fire equal COUNTS; the discrimination is TIMING — the cast lands BEFORE the Full Burst window opens, so the nuke never takes the +50% FB major, milk K5/harran precedent): ONE block, target enemy ('all enemies' collapses to the single partless boss), flatDamage atkPct 152.22 ('Deals 152.22% of final ATK as Burst Skill damage'; burst bucket, crit-eligible by flatDamage convention). The burst's second line 'DEF ▼ 13.32% for 5 sec' (all enemies) is MODELED as a SIBLING burstCast → enemy defPct -13.32 /5s block on the enemy DEF channel (bossDefNow scales cfg.bossDef, floor 0; a sibling rather than a rider so the spec's flatDamage-keyed M6 trigger patcher stays surgical — anis precedent). ~18.6 flat DEF per 5s window at ~25% uptime (her ~20s sole-B1 cycle) at the scope-lock 140 basis — sub-0.1%, live at web raid DEF defaults; no stacking clause → same-caster-slot overwrite refresh. The nuke carries burstDesc 'allEnemies' — her damage block reads '■ Affects all enemies.', the exact string trina's amp names, so she is a true carrier under the literal-only ruling (owner 2026-08-10). The nearest-wrong encoding — laundering the DEF▼ into damageTakenPct (a different mechanic: the boss taking MORE damage) — stays pinned RED in the unit test. Burst gauge: no row in data/gauge-per-shot.json — the class-modal fallback applies; her kit carries no gauge line (flatPerTrigger 0 is faithful). Cadence tuple: RL chargeFrames 60 + reloadFrames 141 + ammo 6 shipped datamine as-is (the cadence-tuple ⚑ was RETIRED by owner ruling 2026-07-25 — datamine tuple reliable; no charFixes). SR RARITY CEILING: original_rare SR → the scope-lock copies:10 ceiling (3★ + core 7) is an SSR ceiling an SR can never reach; the unit test runs unitLimits {stars:3, core:0} (himeno/belorta precedent). NO `ignored` blocks. EVIDENCE TIER: every modeled magnitude is SL10 kit-text-literal (DATAMINED): 2 rounds / 19.89 / 10s (S2), 152.22 (burst); the skill2 interval 20s is DATAMINED (skillCooldownsSec.skill2). TIER 2: scoped top-2-final-ATK buff pool + flat-round weapon-state modifier, the burstCast-vs-fullBurstEnter identity (timing/FB-major discriminated in the sole-B1 fixture), the interval duty cycle, and two out-of-domain ⚑ clusters. ⚑ LIST: [⚑1] (OUT-OF-DOMAIN, incoming-damage subsystem — TIER 2) the entire S1 attacked-20x sentence: estimate = ZERO damage impact at scope lock (the trigger never fires; the effect is self DEF — inert in any case); in a real fight where the boss targets her the window would recur often (10s duration, re-trigger every 20 hits taken), still moving no damage — her v1 omission is costless; recipe = an incoming-damage model to feed the existing `attacked` trigger + a focus video reading hit-accrual cadence (boss-targeting dependent — the sim models no boss targeting either); tier = out-of-domain (admi/jackal attacked-cluster precedent). [⚑2] (interval phase) S2 first-fire at t=20s is the engine-native interval convention (first cast when the cooldown completes); pin from footage if an S2-consumer cadence is ever popup-read; tier = engine-native default (himeno precedent). Kit-autonomy gauntlet 2026-08-05.",
  "unmodeled": {
    "skill1": [],
    "skill2": [],
    "burst": []
  },
  "caveats": [
    "skill1: the whole attacked-20x sentence (self DEF ▲39.18% / 10s) is ENCODED on the `attacked` trigger (makima/yulha precedent) and is dormant by mechanism — the engine's only caller of recordAttack is the manualAttacks test hook, so nothing feeds the counter at scope lock, and the effect (defPct on self) would be damage-inert even if it fired. Present-but-dormant (⚑1), and the unit test pins it BOTH ways: it fires under the test hook, and the hitCount:20 'attacks' misread stays RED.",
    "burst: the DEF ▼13.32%/5s is MODELED as a sibling burstCast → enemy defPct block on the enemy DEF channel (~25% uptime per her ~20s sole-B1 cycle; sub-0.1% at the scope-lock 140-DEF basis, live at web raid DEF defaults); the unit test keeps the damageTakenPct laundering pinned RED. The nuke carries burstDesc 'allEnemies' — the literal clause on her own damage block (amps are literal-only, owner 2026-08-10).",
    "skill2's modeled ammo channel is the kit's only damage lever: the +2-round windows (50% duty cycle, 10s every 20s) lift ONLY the two highest-final-ATK holders' firing uptime; the DEF half is inert. On a basis where mica herself or a low-ATK carry tops the ranking, the holder set re-ranks live per cast (byFinalAtk)."
  ],
  "skill1": [
    {
      "slot": "skill1",
      "trigger": { "kind": "attacked", "count": 20 },
      "target": { "kind": "self" },
      "effects": [
        { "kind": "buff", "stat": "defPct", "value": 39.18, "durationSec": 10 }
      ]
    }
  ],
  "skill2": [
    {
      "slot": "skill2",
      "trigger": { "kind": "interval", "sec": 20 },
      "target": { "kind": "alliesTopAtk", "count": 2, "byFinalAtk": true },
      "effects": [
        {
          "kind": "buff",
          "stat": "maxAmmoFlat",
          "value": 2,
          "durationSec": 10
        },
        { "kind": "buff", "stat": "defPct", "value": 19.89, "durationSec": 10 }
      ]
    }
  ],
  "burst": [
    {
      "slot": "burst",
      "trigger": { "kind": "burstCast" },
      "target": { "kind": "enemy" },
      "effects": [
        { "kind": "flatDamage", "atkPct": 152.22, "burstDesc": "allEnemies" }
      ]
    },
    {
      "slot": "burst",
      "trigger": { "kind": "burstCast" },
      "target": { "kind": "enemy" },
      "effects": [
        { "kind": "buff", "stat": "defPct", "value": -13.32, "durationSec": 5 }
      ]
    }
  ]
}
