# build output
dist/
web/dist/
*.tsbuildinfo

# deps
node_modules/

# lockfiles & generated
package-lock.json

# doc-drift parses these as structured text; prettier's table alignment breaks the census check
docs/STATE.md
docs/engine-modeling-gaps.md
docs/data/damage-bucket-matrix.md

# fully generated by scripts/gen-unmodeled-review.ts, byte-compared by its --check gate in
# verify.sh. Prettier realigns the summary table's columns, so a formatted copy never equals the
# generator's output and the freshness gate would fail on every commit.
docs/unmodeled-entries-review.md

# blind-rebuild extracted engine snippets; not valid standalone TS
scripts/blind-rebuild/code-bundle/*.ts
