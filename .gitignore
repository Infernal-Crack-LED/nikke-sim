# secrets / local env
.env

# local git credential tooling (machine-specific, not project source)
.git-credential-pat.sh

# node
node_modules/

# build output (regenerated from source via vite build)
dist/

docs/probes
docs/handoffs